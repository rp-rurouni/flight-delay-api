Place the trained model bundle here:

flight_delay_bundle.joblib

This file is created in the Colab notebook with:

joblib.dump(bundle, "flight_delay_bundle.joblib")

Download it from Colab and copy it into this folder before final submission.
